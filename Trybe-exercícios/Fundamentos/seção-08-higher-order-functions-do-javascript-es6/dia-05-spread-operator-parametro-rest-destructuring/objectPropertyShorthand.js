const getPosition = (latitude, longitude) => ({
    latitude: latitude,    //só tirar a chave latitude:
    longitude: longitude,   //só tirar a chave longitude:
  });
  
  console.log(getPosition(-19.8157, -43.9542));